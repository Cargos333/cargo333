# Pin Flask to a 2.2.x release for compatibility with Flask-Login 0.6.2
# Flask 2.3+ removed some internal symbols (like _request_ctx_stack) that
# older Flask-Login releases depend on. If you prefer to keep Flask 2.3+,
# upgrade Flask-Login to a release that is explicitly compatible with Flask 2.3+.
Flask==2.2.5
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.2
Werkzeug==2.3.7
gunicorn==21.2.0
python-dotenv==1.0.0
psycopg2-binary==2.9.9
numpy==1.24.3
pandas==2.0.3
openpyxl==3.1.2
